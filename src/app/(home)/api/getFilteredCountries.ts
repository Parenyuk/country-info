"use server"

type PropsType = {
    url: string;
    fields?: string[];
}

// export const getFilteredCountries = async (props: PropsType) => {
//   const { url, fields } = props;
//
//   // const requestUrl = ''
//
//   const data = await fetchAPI(url);
//
//   return data;
// }
